# =============================================================================
# plex-arr-stack — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# All variables referenced in docker-compose.yml are defined here.
# =============================================================================

# --- User / Group IDs --------------------------------------------------------
# Run `id` on your host to find these. Ensures files are owned by your user.
PUID=1000
PGID=1000

# --- Timezone ----------------------------------------------------------------
# Full list: https://en.wikipedia.org/wiki/List_of_tz_database_time_zones
TZ=America/New_York

# --- Paths -------------------------------------------------------------------
# Where service configs are stored (each service gets a subfolder).
CONFIG_DIR=/opt/plex-arr-stack/config

# Root data directory. Should contain `media/` and `torrents/` subdirectories.
# See docs/storage.md for the recommended layout.
DATA_DIR=/opt/plex-arr-stack/data

# --- Plex --------------------------------------------------------------------
# Get a claim token at https://plex.tv/claim (valid for 4 minutes).
PLEX_CLAIM=

# --- *arr API Keys -----------------------------------------------------------
# Generated automatically on first run. Find them in each app's
# Settings → General → API Key. Needed by Unpackerr and Notifiarr.
SONARR_API_KEY=
RADARR_API_KEY=
LIDARR_API_KEY=

# --- Notifiarr ---------------------------------------------------------------
# Get your API key at https://notifiarr.com
NOTIFIARR_API_KEY=
